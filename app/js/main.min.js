const menu=document.querySelector(".menu"),menuBtn=document.querySelector(".menu-btn"),menuLinks=document.querySelectorAll(".menu__list-item"),showMenu=()=>{menu.classList.toggle("menu--active"),menuBtn.classList.toggle("menu-btn--active")};menuBtn.addEventListener("click",showMenu);for(let e of menuLinks)e.addEventListener("click",showMenu);